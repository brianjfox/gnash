build/
build-*/
dist/
*.o
*.a
*.dSYM/
compile_commands.json
.cache/

# Vendored bash test corpus (copied from bash's tests/ for the conformance
# harness) and its compiled helper programs -- not part of gnash's sources.
tests/bash-tests/

# Stray shell binary copies made for testing (e.g. cp gnash bash)
/bash

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
.claude/settings.local.json.*
